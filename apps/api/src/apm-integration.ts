import type { FastifyInstance } from 'fastify';
import { AppError } from '../../../platform/contracts/src/index.js';
import { demoIds, type createEvoRuntime } from './evo-runtime.js';
type Runtime=ReturnType<typeof createEvoRuntime>;
const APM={enterpriseId:'APM',orderNo:'SO-20260918-0182',customer:'Northstar Industrial Systems',product:'Critical Servo Module',materialId:'SERVO-MODULE-CRITICAL',promiseDate:'2026-09-18',revenueAtRisk:280000,experienceDefinitionVersion:'3',supplierA:{id:'SUPPLIER-A',eta:'2026-09-20',revisedDate:'2026-09-20'},supplierB:{id:'SUPPLIER-B',eta:'2026-09-15',priceDeltaPct:8.4,costDeltaPercent:8.4,qualified:true}} as const;
function fail(code:string,message:string):never{throw new AppError({code,message,module:'integration',operation:'apm'});}
function requiredString(value:unknown,code:string,name:string):string{if(typeof value!=='string'||value.length===0)fail(code,`${name} is required.`);return value;}
async function selectedSupplier(runtime:Runtime,enterpriseId:string){const latest=await runtime.db.selectFrom('business_data').select(['id','payload','business_object_version','effective_at']).where('enterprise_id','=',enterpriseId).where('business_data_type','=','procurement.alternate-supplier-selected').where('business_object_key','=',APM.orderNo).orderBy('business_object_version','desc').executeTakeFirst();if(!latest)return null;const payload=latest.payload as Record<string,unknown>;return{businessDataId:latest.id,supplierId:payload.supplierId,version:latest.business_object_version.toString(),effectiveAt:latest.effective_at};}
function stateEtag(selection:Awaited<ReturnType<typeof selectedSupplier>>):string{return selection===null?'state-7':`state-supplier-v${selection.version}`;}
export async function apmObservation(runtime:Runtime){const ids=await demoIds(runtime);const selection=await selectedSupplier(runtime,ids.enterpriseId);const observedAt=new Date().toISOString();const observationId=selection===null?'obs-apm-so-0182-v7':`obs-apm-so-0182-supplier-v${selection.version}`;return{contractVersion:'1.0.0',observationId,deliveryId:`delivery:${observationId}:${observedAt}`,enterpriseId:APM.enterpriseId,observationType:'BUSINESS_FACT',effectiveAt:selection?.effectiveAt??'2026-09-15T08:00:00Z',observedAt,publishedAt:observedAt,subject:{type:'sales-order',key:APM.orderNo},stateEtag:stateEtag(selection),definitionVersion:APM.experienceDefinitionVersion,facts:{orderNo:APM.orderNo,customer:APM.customer,product:APM.product,materialId:APM.materialId,promiseDate:APM.promiseDate,revenueAtRisk:APM.revenueAtRisk,revenueAtRiskUsd:APM.revenueAtRisk,supplierA:APM.supplierA,supplierB:APM.supplierB,marginAfterSupplierB:'acceptable',selectedSupplierId:selection?.supplierId??'SUPPLIER-A',shortageRisk:selection===null},lineage:{sourceSystem:'EVO',sourceBusinessDataId:selection?.businessDataId??null},correlationId:'corr-apm-shortage-001'};}
function serializableCommand(result:{commandExecutionId:string;businessDataId:string;businessObjectVersion:bigint;postingInputId:string;postingSequence:bigint;postingStatus:string;retroactive:boolean;replayRequired:boolean;idempotentReplay:boolean}){return{...result,businessObjectVersion:result.businessObjectVersion.toString(),postingSequence:result.postingSequence.toString()};}
async function executeAlternateSupplier(runtime:Runtime,requestId:string,input:{supplierId:string;idempotencyKey:string;correlationId:string}){const ids=await demoIds(runtime);if(!ids.apmProcurementAppId)fail('APM_PROCUREMENT_NOT_INSTALLED','Run seed:apm:v03 before the APM integration demo.');await runtime.auth.require({enterpriseId:ids.enterpriseId,actorType:'HUMAN',actorId:'demo-user',permissionCode:'procurement.use-alternate-supplier'});if(input.supplierId!=='SUPPLIER-B')fail('APM_ALTERNATE_SUPPLIER_REQUIRED','The reference decision admits only the qualified alternate supplier.');const current=await selectedSupplier(runtime,ids.enterpriseId);const result=await runtime.command.execute({enterpriseId:ids.enterpriseId,applicationInstanceId:ids.apmProcurementAppId,commandCode:'procurement.use-alternate-supplier',actor:{type:'HUMAN',id:'demo-user'},requestId,correlationId:input.correlationId,idempotencyKey:input.idempotencyKey,input:{supplierId:input.supplierId},effectiveAt:new Date(),businessObjectKey:APM.orderNo,expectedBusinessVersion:current===null?0n:BigInt(current.version)});return{command:serializableCommand(result),outcome:{contractVersion:'1.0.0',outcomeId:`outcome:${result.commandExecutionId}`,status:'EXECUTED',commandExecutionId:result.commandExecutionId,businessDataId:result.businessDataId,correlationId:input.correlationId,idempotentReplay:result.idempotentReplay},observation:await apmObservation(runtime)};}
export function registerApmIntegration(app:FastifyInstance,runtime:Runtime):void{
app.get('/api/v1/apm/observation',async()=>apmObservation(runtime));
app.post('/api/v1/apm/command-proposals/admit',async request=>{const body=request.body as Record<string,unknown>;if('authorized'in body||'authorization'in body)fail('UNTRUSTED_AUTHORIZATION_ASSERTION','External proposal cannot assert EVO authorization.');if(body.contractVersion!=='1.0.0')fail('COMMAND_CONTRACT_NOT_SUPPORTED','Command proposal contract is not supported.');if(body.commandContractId!==undefined&&body.commandContractId!=='evo.command')fail('COMMAND_CONTRACT_NOT_SUPPORTED','Command contract id is not supported.');if(body.commandContractVersion!==undefined&&body.commandContractVersion!=='1.0.0')fail('COMMAND_CONTRACT_NOT_SUPPORTED','Command contract version is not supported.');if(body.enterpriseId!==APM.enterpriseId)fail('ENTERPRISE_SCOPE_MISMATCH','Proposal enterprise must be APM.');if(body.commandCode!=='procurement.use-alternate-supplier')fail('COMMAND_NOT_ADMITTED','Command is not admitted by the APM integration profile.');if(body.inputSchemaVersion!=='1')fail('COMMAND_SCHEMA_VERSION_MISMATCH','Proposal schema version is not supported.');const proposalId=requiredString(body.proposalId,'COMMAND_PROPOSAL_ID_REQUIRED','proposalId');if(body.commandIdempotencyKey!==undefined){const key=requiredString(body.commandIdempotencyKey,'COMMAND_IDEMPOTENCY_KEY_REQUIRED','commandIdempotencyKey');if(proposalId===key)fail('PROPOSAL_COMMAND_IDENTITY_COLLISION','proposalId and commandIdempotencyKey are distinct identities.');}if(body.expiresAt!==undefined){const expiry=new Date(requiredString(body.expiresAt,'COMMAND_PROPOSAL_EXPIRY_REQUIRED','expiresAt'));if(Number.isNaN(expiry.getTime())||expiry.getTime()<=Date.now())fail('COMMAND_PROPOSAL_EXPIRED','Command proposal is expired or has invalid expiry.');}const proposedInput=body.proposedInput as Record<string,unknown>|undefined;if(!proposedInput||Object.keys(proposedInput).length!==1||proposedInput.supplierId!=='SUPPLIER-B')fail('COMMAND_PROPOSAL_INPUT_INVALID','The qualified alternate supplier is required.');return{admitted:true,executionStatus:'NOT_EXECUTED',proposalId,commandCode:body.commandCode,proposedInput,correlationId:body.correlationId,authorization:'EVO_REEVALUATES_AT_EXECUTION'};});
app.post('/api/v1/apm/actions/execute',async request=>{const body=request.body as Record<string,unknown>;if('authorized'in body||'authorization'in body)fail('UNTRUSTED_AUTHORIZATION_ASSERTION','ActionRequest cannot assert EVO authorization.');if(body.contractVersion!=='1.0.0'||body.experienceContractVersion!=='1.0.0'||body.capabilityId!=='decision-panel'||body.capabilityVersion!=='1.0.0'||body.actionSemantic!=='APPROVE_RECOMMENDATION')fail('ACTION_CONTRACT_NOT_SUPPORTED','ActionRequest contract/capability/action semantic is not supported.');const target=body.targetRef as Record<string,unknown>|undefined;if(!target||target.host!=='enterprise-runtime'||target.resourceType!=='supplier-decision'||target.resourceId!==APM.orderNo)fail('ACTION_TARGET_NOT_MAPPABLE','Host cannot map this canonical ActionRequest target.');const confirmation=body.confirmationEvidence as Record<string,unknown>|undefined;if(confirmation?.confirmed!==true)fail('HUMAN_CONFIRMATION_REQUIRED','Human confirmation evidence is required.');const values=body.submittedValues as Record<string,unknown>|undefined;if(!values||Object.keys(values).length!==1||values.supplierId!=='SUPPLIER-B')fail('ACTION_INPUT_REQUIRED','submittedValues must select SUPPLIER-B.');const actionRequestId=requiredString(body.actionRequestId,'ACTION_REQUEST_ID_REQUIRED','actionRequestId');const idempotencyKey=`eidos:${actionRequestId}`;const ids=await demoIds(runtime);const prior=await runtime.db.selectFrom('command_execution').select('id').where('enterprise_id','=',ids.enterpriseId).where('idempotency_key','=',idempotencyKey).executeTakeFirst();if(prior===undefined){const selection=await selectedSupplier(runtime,ids.enterpriseId);if(body.presentedStateEtag!==stateEtag(selection))fail('ACTION_PRESENTED_STATE_STALE','Presented state no longer matches authoritative EVO state.');if(body.presentedDefinitionVersion!==undefined&&body.presentedDefinitionVersion!==APM.experienceDefinitionVersion&&body.presentedDefinitionVersion!=='1')fail('ACTION_PRESENTED_DEFINITION_STALE','Presented definition version is not supported by the certified compatibility profile.');}return executeAlternateSupplier(runtime,request.id,{supplierId:'SUPPLIER-B',idempotencyKey,correlationId:String(body.correlationId??'corr-apm-shortage-001')});});
}
